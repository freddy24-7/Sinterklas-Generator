---
name: Git workflow preference
description: User handles all git commits and pushes themselves
type: feedback
---

Never run git commit or git push without explicit instruction from the user.

**Why:** User prefers to manage git themselves.
**How to apply:** After making code changes, tell the user what changed and ask if they want to commit — do not run git commands.
